POSTGRES_USER=
POSTGRES_PASSWORD=

# KeyDB — broker Celery (DB 0) e backend de resultados (DB 1)
KEYDB_HOST=
KEYDB_PORT=6379
KEYDB_BROKER_DB=0
KEYDB_RESULT_DB=1

# Banco legado EOL — string ODBC com ReadOnly=True obrigatório
EOL_DB=

# Bases destino PostgreSQL
POSTGRES_SINC_REC_DB=
POSTGRES_INSTITUCIONAL_DB=
